# Python 基础自测卷

> **用途**：个人自测练习，用于定位自己的 Python 基础薄弱点
> **建议时间**：90 分钟（卷一、卷二、卷三 30 分钟；卷四、卷五 60 分钟）
> **满分**：100 分
> **作答要求**：闭卷；选择、判断、填空、代码阅读题不要上机运行，凭理解作答；编程题可以上机验证

| 题型 | 题量 | 分值 | 建议用时 |
| --- | --- | --- | --- |
| 一、单项选择题 | 20 题 | 40 分 | 20 分钟 |
| 二、判断题 | 10 题 | 10 分 | 10 分钟 |
| 三、填空题 | 10 空 | 20 分 | 15 分钟 |
| 四、代码阅读题 | 3 题 | 15 分 | 20 分钟 |
| 五、编程题 | 3 题 | 15 分 | 25 分钟 |

---

## 一、单项选择题（每题 2 分，共 40 分）

**1.** 下列变量名中，合法的是（ ）

- A. `2name`
- B. `my-name`
- C. `_count`
- D. `class`

**2.** `type(3.14)` 的结果是（ ）

- A. `<class 'int'>`
- B. `<class 'float'>`
- C. `<class 'str'>`
- D. `<class 'double'>`

**3.** 表达式 `10 // 3` 的结果是（ ）

- A. `3.3333333333333335`
- B. `3`
- C. `4`
- D. `1`

**4.** `bool("")` 的值是（ ）

- A. `True`
- B. `False`
- C. 报错
- D. `None`

**5.** 下列**不属于** Python 内置可变类型的是（ ）

- A. `list`
- B. `dict`
- C. `tuple`
- D. `set`

**6.** `"Python"[1:4]` 的结果是（ ）

- A. `"Pyt"`
- B. `"yth"`
- C. `"ytho"`
- D. `"tho"`

**7.** 表达式 `3 * "ab"` 的结果是（ ）

- A. `"ababab"`
- B. `"ab3"`
- C. 报错
- D. `"abab"`

**8.** 下列哪个方法可以正确地向列表末尾添加一个元素？（ ）

- A. `lst.add(1)`
- B. `lst.append(1)`
- C. `lst.push(1)`
- D. `lst.insert(1)`

**9.** `len([1, [2, 3], 4])` 的值是（ ）

- A. `4`
- B. `3`
- C. `2`
- D. `5`

**10.** 已知 `d = {"a": 1, "b": 2}`，要取出键 `"a"` 对应的值，正确写法是（ ）

- A. `d["a"]`
- B. `d("a")`
- C. `d -> a`
- D. `d.get{"a"}`

**11.** 下面代码的输出是（ ）

```python
x = 5
if x > 3:
    print("A")
elif x > 1:
    print("B")
else:
    print("C")
```

- A. `A`
- B. `B`
- C. `AB`
- D. `C`

**12.** `for i in range(1, 5):` 这个循环一共会执行几次？（ ）

- A. 4 次
- B. 5 次
- C. 3 次
- D. 报错

**13.** 关于 `while` 循环，下列说法正确的是（ ）

- A. `while` 循环至少会执行一次循环体
- B. `while` 循环有可能一次都不执行
- C. `while` 循环的条件必须是 `True`
- D. `while` 循环不能和 `else` 一起使用

**14.** 下面函数调用 `f(3, 4)` 的返回值是（ ）

```python
def f(a, b=10):
    return a * b
```

- A. `34`
- B. `12`
- C. `40`
- D. 报错

**15.** 关于变量作用域，下列说法正确的是（ ）

- A. 函数内部可以随意修改外部全局变量，不需要任何声明
- B. 函数内部定义的变量，在函数外部可以直接访问
- C. 函数内部读取（不修改）全局变量是允许的
- D. Python 没有作用域的概念

**16.** 要打开一个文本文件并读取内容，正确写法是（ ）

- A. `open("a.txt", "r")`
- B. `open("a.txt", "read")`
- C. `open("a.txt", "w")`
- D. `open("a.txt", r)`

**17.** `print(1, 2, sep="-")` 的输出是（ ）

- A. `1-2`
- B. `12`
- C. `1 2`
- D. `-1-2`

**18.** 列表推导式 `[x * x for x in range(4)]` 的结果是（ ）

- A. `[0, 1, 4, 9]`
- B. `[1, 4, 9, 16]`
- C. `[0, 1, 2, 3]`
- D. `[1, 2, 3, 4]`

**19.** 关于 `try / except`，下列说法正确的是（ ）

- A. 异常如果不处理，程序一定会崩溃且无法捕获
- B. `except` 可以捕获指定类型的异常
- C. `finally` 代码块只在发生异常时执行
- D. `try` 必须搭配 `finally` 使用

**20.** 表达式 `"1" + 1` 的结果是（ ）

- A. `2`
- B. `"11"`
- C. 抛出 `TypeError`
- D. `1`

---

## 二、判断题（每题 1 分，共 10 分，正确打 √，错误打 ×）

1. Python 中的缩进只是为了好看，不影响程序的运行结果。（ ）
2. `==` 用于比较两个值是否相等，`is` 用于比较两个变量是否指向同一个对象。（ ）
3. 元组（tuple）创建之后，可以修改它的元素。（ ）
4. `input()` 函数返回的数据类型是字符串。（ ）
5. 列表和字符串都支持用 `+` 进行拼接。（ ）
6. `range(5)` 生成的序列中包含数字 5。（ ）
7. 函数如果没有写 `return` 语句，默认返回 `None`。（ ）
8. 集合（set）中可以保存重复的元素。（ ）
9. 使用 `with open("a.txt") as f:` 可以让文件在代码块结束后自动关闭。（ ）
10. `int("3.5")` 可以正常执行，并得到结果 `3`。（ ）

---

## 三、填空题（每空 2 分，共 20 分）

1. 在屏幕上输出 `hello world` 的语句是：`____________`
2. 用来查看某个值的数据类型的内置函数是：`____________`
3. 把字符串 `"123"` 转换成整数 `123` 的写法是：`____________`
4. 已知 `lst = [1, 2, 3]`，用负索引取出最后一个元素 `3` 的写法是：`____________`
5. 已知 `d = {"a": 1}`，向字典中添加键 `"b"`、值 `2` 的语句是：`____________`
6. 在循环中，用于**立即结束整个循环**的关键字是：`____________`
7. 在循环中，用于**跳过本次循环、直接进入下一次循环**的关键字是：`____________`
8. 定义函数时使用的关键字是：`____________`
9. `import math` 之后，调用平方根函数的写法是：`math.sqrt(9)`；那么**直接从 math 中导入 sqrt** 的写法是：`____________`
10. 计算 2 的 10 次方的表达式是：`____________`

---

## 四、代码阅读题（每题 5 分，共 15 分，写出运行结果）

**1.**

```python
s = 0
for i in range(1, 6):
    if i % 2 == 0:
        continue
    s += i
print(s)
```

输出：____________________

**2.**

```python
d = {"a": 1, "b": 2}
d["c"] = d.get("a", 0) + d.get("d", 0)
print(d)
print(len(d))
```

输出：____________________

**3.**

```python
def f(x, lst=[]):
    lst.append(x)
    return lst

print(f(1))
print(f(2))
```

两次 `print` 的输出分别是：____________________

---

## 五、编程题（每题 5 分，共 15 分，写出完整可运行代码）

**1.** 从键盘输入一个整数 `n`，打印 1 到 n（含 n）之间所有能被 3 整除的数，每个数一行。

**2.** 写一个函数 `count_chars(s)`，接收一个字符串，返回一个字典，键是字符，值是它出现的次数。
例如 `count_chars("aba")` 返回 `{"a": 2, "b": 1}`。

**3.** 当前目录下有一个文本文件 `data.txt`，写一段代码读取它，统计并打印文件中的单词总数（提示：`split()` 按空白切分）。

---

## 交卷后的操作

1. 打开 [Python基础自测卷-答案解析.md](./Python基础自测卷-答案解析.md) 逐题对照打分；
2. 把五个题型的得分填到解析文件末尾的「成绩记录表」；
3. 运行自动判分脚本批改卷一、卷二：

```powershell
python quiz.py
```

4. 根据得分区间，对照 [AI方向学习路线图.md](./AI方向学习路线图.md) 第 0 节选择自己的起点。